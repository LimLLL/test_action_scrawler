name: rongheguai

on:
  workflow_dispatch:

jobs:
  scrawler:
    runs-on: ubuntu-latest

    steps:
      - name: 融合怪
        run: export noninteractive=true && curl -L https://raw.githubusercontent.com/oneclickvirt/ecs/master/goecs.sh -o goecs.sh && chmod +x goecs.sh && sudo bash goecs.sh env && sudo bash goecs.sh install && sudo goecs
